# Holdout evidence (not yet collected)

This directory contains no D3 prediction or result. The protocol is dependency- and human-gated. When those gates pass, retain both repeated raw runs, normalized predictions, environment, package hashes, failures, timing samples, metric output, and error taxonomy here. Never delete a failed or inconclusive run.
