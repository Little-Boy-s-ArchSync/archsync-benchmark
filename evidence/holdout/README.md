# Holdout evidence (not yet collected)

This directory contains no D3 prediction or result. The protocol is dependency- and human-gated, and `pnpm holdout:gate` intentionally fails. When those gates pass, retain both repeated raw runs and hashes, normalized predictions and hashes, repository observations, environment, exact Core/Guardian version/commit/artifact hashes, failures, CPU/memory/timing/parsed-scope samples, per-repository and pooled metric denominators, and the required error taxonomy here. Never delete a failed or inconclusive run.
