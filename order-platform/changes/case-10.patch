diff --git a/order-service/src/service.ts b/order-service/src/service.ts
--- a/order-service/src/service.ts
+++ b/order-service/src/service.ts
@@ -1,7 +1,10 @@
+import { publish } from "./events.js";
+
 const paymentServiceUrl = process.env.PAYMENT_SERVICE_URL ?? "http://payment-service:3002";
 
 export async function createOrder(order: unknown): Promise<void> {
   await saveOrder(order);
+  await publish("order.created", order);
   await fetch(`${paymentServiceUrl}/payments`, {
     method: "POST",
     headers: { "content-type": "application/json" },
@@ -12,4 +15,3 @@ export async function createOrder(order: unknown): Promise<void> {
 async function saveOrder(_order: unknown): Promise<void> {
   // Represents Order Service -> PostgreSQL.
 }
-
diff --git a/order-service/src/events.ts b/order-service/src/events.ts
new file mode 100644
--- /dev/null
+++ b/order-service/src/events.ts
@@ -0,0 +1,3 @@
+export async function publish(topic: string, payload: unknown): Promise<void> {
+  console.info("publish", { topic, payload });
+}
diff --git a/order-worker/src/worker.ts b/order-worker/src/worker.ts
new file mode 100644
--- /dev/null
+++ b/order-worker/src/worker.ts
@@ -0,0 +1,3 @@
+export async function handleOrderCreated(order: unknown): Promise<void> {
+  console.info("processing order asynchronously", order);
+}
