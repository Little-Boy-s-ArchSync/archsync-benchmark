diff --git a/order-service/src/service.ts b/order-service/src/service.ts
--- a/order-service/src/service.ts
+++ b/order-service/src/service.ts
@@ -1,7 +1,11 @@
+import { getCachedOrder, setCachedOrder } from "./cache.js";
+
 const paymentServiceUrl = process.env.PAYMENT_SERVICE_URL ?? "http://payment-service:3002";
 
 export async function createOrder(order: unknown): Promise<void> {
+  if (await getCachedOrder(order)) return;
   await saveOrder(order);
+  await setCachedOrder(order);
   await fetch(`${paymentServiceUrl}/payments`, {
     method: "POST",
     headers: { "content-type": "application/json" },
@@ -12,4 +16,3 @@ export async function createOrder(order: unknown): Promise<void> {
 async function saveOrder(_order: unknown): Promise<void> {
   // Represents Order Service -> PostgreSQL.
 }
-
diff --git a/order-service/src/cache.ts b/order-service/src/cache.ts
new file mode 100644
--- /dev/null
+++ b/order-service/src/cache.ts
@@ -0,0 +1,11 @@
+import { createClient } from "redis";
+
+const redis = createClient({ url: process.env.REDIS_URL });
+
+export async function getCachedOrder(order: unknown): Promise<boolean> {
+  return (await redis.get(JSON.stringify(order))) !== null;
+}
+
+export async function setCachedOrder(order: unknown): Promise<void> {
+  await redis.set(JSON.stringify(order), "1", { EX: 60 });
+}
